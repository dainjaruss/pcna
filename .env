# Database
POSTGRES_PASSWORD=P9kM2qR8vT7wE5zB
DATABASE_URL="postgresql://popculture:${POSTGRES_PASSWORD}@localhost:5432/popculture_db?schema=public"

# Email Configuration (Choose one: SMTP or SendGrid)
# Option 1: SMTP (Gmail, Outlook, etc.)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="dainjaruss@gmail.com"
SMTP_PASSWORD="bnxf qbxq dxpl elud"
SMTP_FROM="dainjaruss@gmail.com"

# Option 2: SendGrid (Recommended for production)
# SENDGRID_API_KEY="your-sendgrid-api-key"
# SENDGRID_FROM="noreply@yourdomain.com"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="production"

# Cron Job Configuration
DAILY_EMAIL_TIME="08:00" # 24-hour format (used by built-in cron or for reference)
REFRESH_INTERVAL="6" # hours (used by built-in cron or for reference)

# Default Email Recipients (comma-separated)
DEFAULT_EMAIL_RECIPIENTS="dain.franklyn@icloud.com,jherria@hotmail.com"

# Scheduling Options
# Set to "true" to use built-in cron jobs (default: false)
# Set to "false" to use external scheduler like n8n (recommended)
USE_BUILTIN_CRON="false"

# API Security
# Generate a secure random key: openssl rand -base64 32
# This key is required for external schedulers (like n8n) to call the API endpoints
API_KEY="your-secure-random-api-key-here"
